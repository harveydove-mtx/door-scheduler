{
  "name": "door-scheduler-v2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Matrix Hardware Door Scheduler V2. Only the pure domain logic (pricing engine) exists so far; UI and Firebase come after the data model is signed off.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "typescript": "5.9.3",
    "vitest": "5.0.1"
  }
}
